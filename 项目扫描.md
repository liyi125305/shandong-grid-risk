# 山东电网天气风险看板 — 项目扫描

> 用途：中断后快速恢复上下文，继续开发。

## 一句话
面向电力交易员的简化版山东电网拓扑 + 天气风险分析原型，输出终端预警和 Web 可视化看板。

## 目标用户
电力交易员、市场分析师。

## 当前阶段
- 已完成：简化拓扑建模、Open-Meteo 15 天预报接入、规则化风险识别、Web 看板可视化。
- 暂未完成：真实电网数据接入、输电断面（断面）精细化分析、基于真实电价的预测模型。

## 技术栈
- Python 3.13（venv 在 `.venv/`）
- requests / pandas / matplotlib / networkx
- 前端：ECharts + 原生 JS（无框架）

## 目录结构
```
shandong-grid-risk/
├── main.py                 # 入口：拉天气、评估风险、生成 web/data.json
├── requirements.txt        # Python 依赖
├── README.md               # 对外运行说明
├── shandong.json           # 山东省界 GeoJSON（原始文件）
├── output/                 # 静态拓扑图输出
│   └── topology.png
├── src/
│   ├── topology.py         # 节点（受端/供端）和线路定义
│   ├── weather.py          # Open-Meteo 15 天预报接口
│   ├── risk.py             # 规则化风险识别与综合电价风险
│   ├── visualize.py        # matplotlib 静态拓扑图
│   └── export_web_data.py  # 生成 web/data.json
└── web/                    # Web 看板
    ├── index.html          # 交互式 ECharts 看板（ fetch 加载本地 JSON）
    ├── screenshot.html     # 内联数据版静态页面，用于无头截图
    ├── data.json           # 15 天天气 + 风险 + 电价指数
    ├── topology.json       # 拓扑 JSON
    ├── shandong.json       # 省界 GeoJSON
    ├── echarts.min.js      # 本地 ECharts 库
    └── screenshot.png      # 静态预览截图
```

## 核心数据流
1. `main.py` 遍历 `src/topology.py` 所有节点，从 Open-Meteo 获取 15 天预报。
2. `src/risk.py` 根据规则识别风险：
   - 受端高温：`tmax > 33°C` 中风险 / `tmax > 36°C` 高风险
   - 供端降雨：`precip >= 5mm` 中风险 / `precip >= 20mm` 高风险
3. `src/export_web_data.py` **复用**第 1 步数据生成 `web/data.json`（避免重复请求 API）。
4. 启动 `web/` HTTP 服务器，浏览器打开 `index.html` 查看交互看板。

## 运行方式
```bash
cd /Users/liyi/Documents/vibe/shandong-grid-risk
.venv/bin/python main.py          # 生成 output/topology.png 和 web/data.json
cd web
python3 -m http.server 8081       # 绑定 0.0.0.0，局域网可访问
# 浏览器打开 http://dailymeteo.local:8081/index.html（本机也可 http://localhost:8081）
```

## 当前状态
- 数据已更新至：**2026-07-02**（未来 15 天：07-02 ~ 07-16）。
- Web 服务端口：**8081**，局域网内通过 `dailymeteo.local:8081` 访问。
- 拓扑为**简化示意模型**，非真实山东电网设备参数。
- 风险规则与电价指数（基准 350）均为演示规则，**不构成交易建议**。

## 已知注意
- `index.html` 通过 `fetch` 加载本地 JSON，必须走 HTTP 服务器，不能直接用 `file://` 打开。
- `screenshot.html` 是内联数据 + ECharts 的静态版，用于无头浏览器截图；日常开发以 `index.html` 为准。

## 最近改动
- 接入受端地市人口数据（第七次人口普查），按人口加权需求侧风险。
- 右侧风险卡片新增“受影响人口（万人）”。
- 受端节点大小随人口权重轻微缩放。
- Web 拓扑 JSON 由 `src/export_web_data.py` 自动生成。
- 修复 `main.py` 缩进问题。
- `export_web_data.py` 改为复用 `main.py` 已获取的天气数据，避免对 Open-Meteo 重复请求。
- ECharts 改为本地 `echarts.min.js`，去除 CDN 依赖。
- 生成 `screenshot.html` 用于静态截图。

## 下一步可做的事
- 接入真实交易价格或省级电价数据，替换当前 350 元基准的示意电价指数。
- 增加**输电断面**概念：将多条线路/多个节点组合为断面，评估断面阻塞和天气影响。
- 加入更多天气因子（风速、湿度、辐照度）和更细化的风险模型。
- 为 `index.html` 增加“单日详情”面板，列出具体触发节点和成因。
- 用 Playwright/Selenium 替换当前无头截图方案，避免维护 `screenshot.html` 双份逻辑。


## 人口数据与需求侧权重

- 文件：`data/population.json`
- 来源：第七次全国人口普查（2020）山东省 9 个受端地市常住人口
- 用途：在 `src/topology.py` 中加载并计算 `population_weight = population / avg_population`，用于加权需求侧高温风险。
- 更新：运行 `scripts/scrape_population.py`（默认复用静态数据，可接 `--url`）。
- 效果：受端高温风险按 `population_weight` 加权后影响电价指数；`src/risk.py` 输出每日受影响人口（万人）；`web/index.html` 风险卡片展示当日受影响人口，tooltip 展示受端城市人口，节点大小随人口权重轻微缩放。
## 输电断面

- 文件：`src/topology.py` 中新增 `CROSS_SECTIONS`。
- 断面列表：鲁中受端断面、胶东受端断面、鲁南受端断面、外电入鲁断面、西部风光送出断面、北部风光送出断面。
- 计算：`src/risk.py` 中 `assess_cross_section_risk` 根据断面内节点天气（受端高温、供端降雨）计算简化负载率与风险等级。
- 导出：`src/export_web_data.py` 在 `web/data.json` 增加 `cross_sections`，在 `web/topology.json` 增加断面元数据。
- 前端：`web/index.html` 右侧面板新增“输电断面”列表，按利用率排序，显示负载、容量、利用率进度条与风险颜色。
- 说明：断面容量与成员为示意值，非真实电网设备参数；断面风险为规则化交易辅助信号，不构成交易建议。

## 公网部署方案

- 推荐：**GitHub Actions + GitHub Pages**
- 文件：`.github/workflows/deploy.yml`
- 机制：每天北京时间 14:00（UTC 06:00）自动运行 `main.py` 拉取 Open-Meteo 数据，生成 `web/data.json`，然后自动部署 `web/` 目录到 GitHub Pages
- 优点：免费，无需额外申请 Cloudflare/Vercel API token，使用 GitHub 自带 `GITHUB_TOKEN`
- 公网地址：`https://<用户名>.github.io/shandong-grid-risk/`
- 启用步骤：创建 GitHub 仓库 → push 项目到 `main` 分支 → Settings > Pages 选择 GitHub Actions 部署来源
- 手动/脚本推送到第三方平台（如 Cloudflare/Vercel）需要管理 API token、定时任务和失败重试，流程更琐碎
